{
  "name": "@ETHChess/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ GITHUB_USERNAME/ETHChessMatches",
    "create-local": "graph create --node http://localhost:8020/ ETHChess/ETHChessMatches",
    "remove-local": "graph remove --node http://localhost:8020/ ETHChess/ETHChessMatches",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ETHChess/ETHChessMatches"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.29.1",
    "@graphprotocol/graph-ts": "^0.26.0"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
